# Вывести сообщения с последнего запуска системы.
# Вывести сообщения не старше одного часа.

Ответ:

Все сообщения:
# [root@slonit-test ~]# journalctl

Посмотреть все сообщения с последнего запуска,
относящиеся только к текущей загрузке системы:
# [root@slonit-test ~]# journalctl -b


Сообщения не старше одного часа:
# [root@slonit-test ~]# journalctl --since "1 hour ago"

По желанию можно добавить -b для текущей загрузки:
# [root@slonit-test ~]# journalctl -b --since "1 hour ago"
